# Project Context

This repository contains Sophon TPU model conversion, compilation, runtime,
and validation work. Model weights and generated deployment artifacts are
large and are intentionally kept outside handoff memory unless explicitly
requested.

Task-specific state is stored below `agent-docs/tasks/`.
